README janix-batch-probability-tester (jxptbatch)

jxptbatch reads a table (jxpttab) in the current working directory, and for
each line, parses the record into three fields: a denominator, a numerator,
and a URL. Then for each line from the URL, a probability test (jxpt) is
performed using the supplied numerator and denominator which, if true, echos
the line to standard output.

# -------------------------------------------------------------------------
# sample jxpttab
#
# Fields:
# 1: denominator
# 2: numerator
# 3: URL source file
# -------------------------------------------------------------------------
14 1 file:///path/to/a/list/of/elements
30 1 file:///path/to/another/list/of/elements
